-- 以下为 AI 生成的文稿笔记内容 --

### 一、 三种交换方式的性能分析 ﻿00:01﻿

本视频将分析电路交换、报文交换和分组交换的数据传输性能。通过图示分析每种交换方式在每一时刻的具体行为。计算机网络课程中此类图示常见且有助于解决真题，课本中提供了对应三种交换方式的图示。为增强直观性，图示中添加了背景小格子，纵轴表示时间递增方向，每个小格子对应 $1 \text{ ms}$。

#### 1. 电路交换性能分析 ﻿00:56﻿

电路交换流程分析：

- 建立连接：
  - 计算机 $A$ 通过节点 $B$、$C$ 与计算机 $D$ 建立连接，传输 $4 \text{ KB}$ 报文。
  - 信号传输时延：每跳（$A \rightarrow B$、$B \rightarrow C$、$C \rightarrow D$）需 $1 \text{ ms}$。
  - 交换机内部连接建立耗时：$B$、$C$ 节点各需 $1 \text{ ms}$，$D$ 节点需 $2 \text{ ms}$ 处理请求。
  - 应答信号返回路径（$D \rightarrow C \rightarrow B \rightarrow A$）每跳 $1 \text{ ms}$。
- 数据传输：
  - 数据传输速率：$0.5 \text{ KB/ms}$，$4 \text{ KB}$ 报文需 $8 \text{ ms}$ 发送完成。
  - 第一个比特从 $A \rightarrow D$ 总耗时：$3 \text{ ms}$（$A \rightarrow B \rightarrow C \rightarrow D$ 各 $1 \text{ ms}$）。
  - 总传输时间：$8 \text{ ms}$（发送） $+ 3 \text{ ms}$（传播） $= 11 \text{ ms}$。
- 释放连接：
  - 释放信号传输与建立连接对称，每跳 $1 \text{ ms}$。

关键点：

- 物理线路独占：数据传输全程占用固定路径。
- 时延组成：传播时延（每跳 $1 \text{ ms}$） $+ 1 \text{ ms}$ 发送时延（$8 \text{ ms}$）。

#### 2. 报文交换性能分析 ﻿09:53﻿

报文交换流程分析（条件同电路交换）：

- $A \rightarrow B$ 传输：
  - 发送 $4 \text{ KB}$ 报文需 $8 \text{ ms}$，第一个比特到达 $B$ 需 $1 \text{ ms}$（传播时延）。
  - $B$ 节点接收完整报文后，存储转发时延为 $2 \text{ ms}$。
- $B \rightarrow C$ 传输：
  - 发送报文需 $8 \text{ ms}$，$C$ 节点存储转发时延 $2 \text{ ms}$。
- $C \rightarrow D$ 传输：
  - 发送报文需 $8 \text{ ms}$，传播时延 $1 \text{ ms}$。

总耗时：

- $A \rightarrow B$：$8 \text{（发送）} + 1 \text{（传播）} + 2 \text{（处理）} = 11 \text{ ms}$
- $B \rightarrow C$：$8 + 1 + 2 = 11 \text{ ms}$
- $C \rightarrow D$：$8 + 1 = 9 \text{ ms}$
- 累计：$11 + 11 + 9 = 31 \text{ ms}$（注：原文计算可能有误，按步骤累加应为 $31 \text{ ms}$，但若 D 节点处理不计入则为 $29 \text{ ms}$）

关键点：

- 存储转发机制：每个节点需接收完整报文后再转发。
- 时延瓶颈：处理时延（$2 \text{ ms/节点}$）显著增加总耗时。

#### 3. 分组交换性能分析 ﻿12:58﻿

分组交换流程分析（报文拆分为 $4$ 个 $1 \text{ KB}$ 分组）：

- 分组传输参数：
  - 每组发送耗时：$2 \text{ ms}$（$0.5 \text{ KB/ms} \times 1 \text{ KB}$）。
  - 存储转发时延：$0.5 \text{ ms/分组}$（较报文交换更短）。
- 流水线传输：
  - $A \rightarrow B \rightarrow C \rightarrow D$ 路径上，分组间隔 $2 \text{ ms}$ 连续发送。
  - 首个分组到达 $D$ 时间：
    - $A \rightarrow B$：$2 \text{（发送）} + 1 \text{（传播）} + 0.5 \text{（处理）} = 3.5 \text{ ms}$
    - $B \rightarrow C$：$2 + 1 + 0.5 = 3.5 \text{ ms}$
    - $C \rightarrow D$：$2 + 1 = 3 \text{ ms}$
    - 累计：$3.5 + 3.5 + 3 = 10 \text{ ms}$
  - 末分组完成时间：$10 \text{ ms} + (3 \text{ 分组} \times 2 \text{ ms 间隔}) = 16 \text{ ms}$

关键点：

- 分组长度的优势：减少节点处理时延，实现并行传输。
- 总耗时对比：电路交换（$11 \text{ ms}$） $<$ 分组交换（$16 \text{ ms}$） $<$ 报文交换（$29 \text{/ } 31 \text{ ms}$）。

### 输出处理结果：

#### 4. 三种交换方式的性能对比

#### ﻿16:06﻿

对三种数据交换方式的传输效率进行对比分析：

- 电路交换：全程耗时 $11 \text{ ms}$
- 报文交换：全程耗时 $29 \text{/ } 31 \text{ ms}$
- 分组交换：全程耗时 $16 \text{ ms}$

结论：

- 电路交换耗时最少，因其无存储转发环节
- 报文交换耗时最多，因需完整存储转发大容量报文
- 分组交换性能居中，其存储转发时延低于报文交换但高于电路交换

#### - 知识回顾与重要考点

#### ﻿17:04﻿

通过多维对比表格总结三种交换方式的特性差异，关键维度包括：

| 对比维度     | 最优方式（😎） | 次优方式（😊） | 最差方式（😢） |
| ------------ | ------------- | ------------- | ------------- |
| 传输时延     | 电路交换      | 分组交换      | 报文交换      |
| 存储转发效率 | 电路交换      | 分组交换      | 报文交换      |

重点提示：

- 图示分析法是计算机网络课程的核心方法，需熟练掌握
- 对比表格中的表情符号具有固定含义：😎 表示最优，😊 表示中等，😢 表示最差
- 此类分析工具可直接应用于真题解析与课后习题