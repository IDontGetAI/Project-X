-- 以下为AI生成的文稿笔记内容 --

### 一、三种交换方式的性能分析 ﻿00:01﻿

本视频将分析电路交换、报文交换和分组交换的数据传输性能。通过图示分析每种交换方式在每一时刻的具体行为。计算机网络课程中此类图示常见且有助于解决真题，课本中提供了对应三种交换方式的图示。为增强直观性，图示中添加了背景小格子，纵轴表示时间递增方向，每个小格子对应1毫秒。 

#### 1.电路交换性能分析 ﻿00:56﻿

电路交换流程分析： 

- 建立连接： 
  - 计算机A通过节点B、C与计算机D建立连接，传输4 KB报文。 
  - 信号传输时延：每跳（A→B、B→C、C→D）需1毫秒。 
  - 交换机内部连接建立耗时：B、C节点各需1毫秒，D节点需2毫秒处理请求。 
  - 应答信号返回路径（D→C→B→A）每跳1毫秒。 
- 数据传输： 
  - 数据传输速率： - 5 KB/毫秒，4 KB报文需8毫秒发送完成。 
  - 第一个比特从A→D总耗时：3毫秒（A→B→C→D各1毫秒）。 
  - 总传输时间：8毫秒（发送） + 3毫秒（传播） = 11毫秒。 
- 释放连接： 
  - 释放信号传输与建立连接对称，每跳1毫秒。 

关键点： 

- 物理线路独占：数据传输全程占用固定路径。 
- 时延组成：传播时延（每跳1毫秒） + 发送时延（8毫秒）。 

#### 2.报文交换性能分析 ﻿09:53﻿

报文交换流程分析（条件同电路交换）： 

- A→B传输： 
  - 发送4 KB报文需8毫秒，第一个比特到达B需1毫秒（传播时延）。 
  - B节点接收完整报文后，存储转发时延为2毫秒。 
- B→C传输： 
  - 发送报文需8毫秒，C节点存储转发时延2毫秒。 
- C→D传输： 
  - 发送报文需8毫秒，传播时延1毫秒。 

总耗时： 

- A→B：8（发送） + 1（传播） + 2（处理） = 11毫秒 
- B→C：8 + 1 + 2 = 11毫秒 
- C→D：8 + 1 = 9毫秒 
- 累计：11 + 11 + 9 = 29毫秒 

关键点： 

- 存储转发机制：每个节点需接收完整报文后再转发。 
- 时延瓶颈：处理时延（2毫秒/节点）显著增加总耗时。 

#### 3.分组交换性能分析 ﻿12:58﻿

分组交换流程分析（报文拆分为4个1 KB分组）： 

- 分组传输参数： 
  - 每组发送耗时：2毫秒（ - 5 KB/毫秒 × 1 KB）。 
  - 存储转发时延： - 5毫秒/分组（较报文交换更短）。 
- 流水线传输： 
  - A→B→C→D路径上，分组间隔 - 5毫秒连续发送。 
  - 首个分组到达D时间： 
    - A→B：2（发送） + 1（传播） + - 5（处理） = - 5毫秒 
    - B→C：2 + 1 + - 5 = - 5毫秒 
    - C→D：2 + 1 = 3毫秒 
    - 累计： - 5 + - 5 + 3 = 10毫秒 
  - 末分组完成时间：10毫秒 + (3分组 × - 5毫秒间隔) = - 5毫秒 

关键点： 

- 分组长度的优势：减少节点处理时延，实现并行传输。 
- 总耗时对比：分组交换（ - 5毫秒） < 电路交换（11毫秒） < 报文交换（29毫秒）。

### 输出处理结果：

#### 4.三种交换方式的性能对比

#### ﻿16:06﻿

对三种数据交换方式的传输效率进行对比分析： 

- 电路交换：全程耗时11毫秒 
- 报文交换：全程耗时29毫秒 
- 分组交换：全程耗时 - 5毫秒 

结论： 

- 电路交换耗时最少，因其无存储转发环节 
- 报文交换耗时最多，因需完整存储转发大容量报文 
- 分组交换性能居中，其存储转发时延低于报文交换但高于电路交换 

#### - 知识回顾与重要考点

#### ﻿17:04﻿

通过多维对比表格总结三种交换方式的特性差异，关键维度包括： 

| 对比维度     | 最优方式（😎） | 次优方式（😊） | 最差方式（😢） |
| ------------ | ------------- | ------------- | ------------- |
| 传输时延     | 电路交换      | 分组交换      | 报文交换      |
| 存储转发效率 | 电路交换      | 分组交换      | 报文交换      |

重点提示： 

- 图示分析法是计算机网络课程的核心方法，需熟练掌握 
- 对比表格中的表情符号具有固定含义：😎表示最优，😊表示中等，😢表示最差 
- 此类分析工具可直接应用于真题解析与课后习题 