-- 以下为AI生成的文稿笔记内容 --

### 一、TCP/IP模型 ﻿00:04﻿

TCP/IP模型是计算机网络体系结构中的重要概念，考试要求掌握其各层名称、顺序以及与OSI参考模型的设计理念差异。 

#### 1.记住各层的名称和顺序 ﻿00:35﻿

##### 1) TCP/IP模型 ﻿00:39﻿

TCP/IP模型共分为四层： 

- 网络接口层：功能类似于OSI参考模型的物理层和数据链路层 
- 网络层：功能与OSI参考模型的网络层类似 
- 传输层：功能与OSI参考模型的传输层类似 
- 应用层：功能相当于OSI参考模型的会话层、表示层和应用层 

记忆口诀为"接网输用"，分别对应： 

- 接：网络接口层 
- 网：网络层 
- 输：传输层 
- 用：应用层 

##### 2) 物联网谁会使用 ﻿01:22﻿

记忆口诀"接网输用"对应TCP/IP模型的四个层次： 

- 接：网络接口层 
- 网：网络层 
- 输：传输层 
- 用：应用层 

#### 2.TCP/IP模型和OSI参考模型在设计理念上的区别 ﻿02:02﻿

TCP/IP模型与OSI参考模型在设计理念上存在显著差异。 

##### 1) 回顾OSI参考模型 ﻿02:10﻿

OSI参考模型的第五层（会话层）和第六层（表示层）功能分析： 

- 会话层：提供会话管理功能（如断点续传） 
- 表示层：提供数据格式转换功能 

关键点： 

- 这些功能并非所有应用程序都必需 
- 数据格式转换在相同编码格式的系统中不必要 
- 会话管理在网络恢复时可选择重新传输而非断点续传 

因此，TCP/IP模型去掉了这两个层次，使网络体系结构更简洁高效。 

##### 2) TCP/IP模型 ﻿04:50﻿

TCP/IP模型将OSI参考模型中会话层和表示层的功能移至应用层实现： 

- 优势：应用程序可根据需要灵活选择是否使用这些功能 
- 对比：OSI参考模型强制所有应用程序经过表示层和会话层处理 

##### 3) 回顾OSI参考模型#1~#2层 ﻿06:20﻿

OSI参考模型对物理层和数据链路层的规定： 

- 物理层：明确定义接口参数和信号含义 
- 数据链路层：实现差错控制和流量控制 

局限性： 

- 限制硬件厂商的创新空间 
- 难以适应通信技术的快速发展 

##### 4) TCP/IP模型的网络接口层 ﻿08:25﻿

TCP/IP模型网络接口层的特点： 

- 功能：仅规定需实现相邻节点间的数据传输 
- 优势： 
  - 不限定具体实现方式 
  - 为硬件厂商提供创新空间 
  - 增强模型的灵活性和适应性 

阶段性总结： 

- TCP/IP模型去除OSI中非必需层次（会话层、表示层） 
- 网络接口层不限定具体实现，促进技术发展 
- 这种设计使TCP/IP模型获得广泛商业支持

#### 3.总结TCP/IP各层功能 ﻿19:15﻿

TCP/IP模型相比OSI参考模型层次更少且更简洁。TCP/IP模型分为四个层次，各层次需完成以下任务及功能：

- 网络接口层：对应OSI参考模型的物理层和数据链路层功能
- 网络层：不包含差错控制、流量控制、连接管理和可靠传输管理功能（与OSI参考模型的主要区别）
- 传输层：实现数据传输可靠性
- 应用层：对应OSI参考模型的会话层、表示层和应用层功能

#### 4.OSI参考模型和TCP/IP模型的网络层功能对比 ﻿20:07﻿

##### 1) OSI参考模型与TCP/IP模型各层对应关系

OSI参考模型的会话层、表示层和应用层对应TCP/IP模型的应用层。

##### 2) TCP/IP模型传输层与网络层功能 ﻿20:18﻿

OSI参考模型的物理层和数据链路层对应TCP/IP模型的网络接口层。

##### 3) OSI参考模型与TCP/IP模型网络层设计理念区别 ﻿20:28﻿

| 对比维度   | OSI参考模型网络层                                            | TCP/IP模型网络层                                             |
| ---------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 功能支持   | 必须实现差错控制、流量控制、连接管理和可靠传输管理           | 不支持上述功能                                               |
| 服务类型   | 可提供有连接可靠服务（虚电路交换）和无连接不可靠服务（数据报交换） | 仅提供无连接不可靠服务                                       |
| 可靠性保障 | 网络层自身保障                                               | 由传输层实现可靠性（TCP协议提供有连接可靠服务，UDP协议提供无连接不可靠服务） |
| 设计背景   | 20世纪80年代设计，要求传输层必须提供有连接可靠服务           | 适应现代网络应用需求，允许传输层提供无连接不可靠服务（如直播等实时应用） |

考试重点：两种体系结构的网络层和传输层功能对比是高频考点，需重点关注服务类型差异和可靠性实现机制。随着课程深入，将对数据传输可靠性机制有更直观理解。