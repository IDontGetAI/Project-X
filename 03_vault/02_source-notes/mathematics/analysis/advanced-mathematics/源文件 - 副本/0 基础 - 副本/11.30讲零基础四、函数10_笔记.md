---
以下为 AI 生成的文稿笔记内容
---

### 一、函数基础概念 00:03

函数概念基于前三部分知识进行总结，主要应用于以下三个领域：

#### 1. 函数应用场景 00:15

##### 1) 基本逻辑问题 00:26
涉及：
- 充要条件
- 数学归纳法
- 反证法
- 逆否命题

##### 2) 解析式概念 00:50
包含完整的概念描述和计算方法。

##### 3) 方程与不等式 01:02
涉及多种函数形式：
- 对数函数
- 幂函数
- 指数函数
函数图像具有对称性特征，将在后续内容中重点讨论。

### 二、幂函数 02:04

#### 1. 幂函数定义 02:08
幂函数定义为 $y = x^\mu$（$\mu$ 为实数）。

#### 2. 常见幂函数 02:26
常见幂函数包括：
- $y = x$
- $y = x^2$
- $y = \sqrt{x}$
- $y = x^3$
- $y = x^{1/3}$
- $y = 1/x$

##### 1) $y = 1/x$ 02:39
$y = 1/x$ 的图像和表达式需要重点掌握。

### 三、指数函数 02:51

#### 1. 指数函数定义 02:53
指数函数定义为 $y = a^x$ ($a > 0$ 且 $a \ne 1$)。当 $a > 1$ 时为增函数，$0 < a < 1$ 时为减函数。

#### 2. 常用指数函数 03:12
常用指数函数为 $y = e^x$，其与 $y$ 轴交点为 $(0, 1)$。
指数转换公式：$u^v = e^{v \ln u}$。

#### 3. 指数运算法则 04:31
- $a^\alpha \cdot a^\beta = a^{\alpha + \beta}$
- $(a^\alpha)^\beta = a^{\alpha\beta}$
- $(ab)^\alpha = a^\alpha b^\alpha$

##### 1) 指数取值范围 04:46
- 当 $x$ 为正整数时，$a$ 可为任意实数。
- 当 $x$ 为整数时，$a$ 为非零实数。
- 当 $x$ 为任意实数时，$a$ 必须为正实数。

### 四、对数函数 09:22

#### 1. 对数函数定义 09:24
对数函数 $y = \log_a x$ 是指数函数 $y = a^x$ 的反函数，图像关于 $y = x$ 对称。

#### 2. 自然对数 10:06
自然对数 $y = \ln x$ 是以 $e$ 为底的对数函数，是 $y = e^x$ 的反函数。

#### 3. 对数运算法则 10:58

##### 1) 积变和 11:12
乘积的对数等于对数的和：$\ln(uv) = \ln u + \ln v$。

##### 2) 商变差 11:24
商的对数等于对数的差：$\ln(u/v) = \ln u - \ln v$。

##### 3) 幂次变倍数 11:33
幂的对数等于对数的倍数：$\ln(u^v) = v \ln u$。

### 五、对称问题 15:04

对称问题是考研数学的重点内容，与高考要求一致。

#### 1. 对称轴与中心 15:08
- **对称轴**：如直线 $x = 0$ ($y$ 轴)。
- **对称中心**：如坐标原点 $(0, 0)$。
- **对称性分类**：轴对称与中心对称是两种基本对称形式。

#### 2. $y$ 轴对称 15:46

| 对称类型 | 对称条件 | 函数性质 |
| :--- | :--- | :--- |
| $y$ 轴对称 | $f(x) = f(-x)$ | 偶函数 |

- **几何特征**：点 $(x, y)$ 与 $(-x, y)$ 关于 $y$ 轴对称。
- **函数关系**：$y = f(x)$ 关于 $y$ 轴对称时，其表达式满足 $f(x) = f(-x)$。

#### 3. 偶函数性质 17:30
- **代数定义**：满足 $f(x) = f(-x)$ 的函数称为偶函数。
- **方程对称**：方程 $f(x, y) = 0$ 关于 $y$ 轴对称时，等价于 $f(-x, y) = 0$。
- **图像特征**：偶函数图像关于 $y$ 轴对称。

### 六、对称变换 17:39

#### 1. 对称变换定义
- **方程变换**：将 $y = f(x)$ 转化为 $f(x, y) = 0$ 形式。
- **对称判定**：通过变量替换判断方程对称性。

##### 1) 轴对称变换 17:53
- **变换规则**：方程 $f(x, y) = 0$ 关于 $y$ 轴对称的条件为 $f(-x, y) = 0$。
- **函数特例**：当 $y = f(x)$ 为显函数时，等价于验证 $f(x) = f(-x)$。

##### 2) 中心对称变换 18:04
- **变换规则**：方程 $f(x, y) = 0$ 关于原点对称的条件为 $f(-x, -y) = 0$。
- **函数特例**：奇函数满足 $f(-x) = -f(x)$ 的中心对称性质。

#### 2. 例题：绝对值不等式 18:39
**解题步骤**：
1. **函数分析**：绘制 $y = |x|$ 图像，为 V 型折线。
2. **不等式转化**：$|x| \ge ax$ 转化为图形位置关系。
3. **斜率范围**：通过几何观察确定 $a \in [-1, 1]$。
**关键结论**：当直线 $y = ax$ 斜率绝对值不超过 $1$ 时，不等式对所有实数 $x$ 成立。

#### 3. 例题：分段函数交点问题 20:41
**解题步骤**：
1. **函数化简**：将 $y = \frac{|x^2-1|}{x-1}$ 分段表示为：
   - $x > 1$ 时，$y = x + 1$。
   - $-1 \le x < 1$ 时，$y = -(x + 1)$。
   - $x < -1$ 时，$y = x + 1$。
2. **图像绘制**：在 $x \ne 1$ 处形成分段线性函数。
3. **交点分析**：通过斜率 $k$ 的范围确定交点数量：
   - $k \in (0, 1) \cup (1, 2)$ 时恰有两个交点。
**易错点**：需特别注意 $x = 1$ 处的函数定义域限制。

#### 4. 作业：幂函数不等式 29:46
**解题要点**：
1. **定义域**：$x > 0$（分母含 $\sqrt{x}$）。
2. **不等式转化**：$x^{3/2} < 1$ 转化为 $0 < x < 1$。
3. **分段求解**：
   - 第一段：$e^{x-1} \le 2$ 且 $x < 1 \implies x < 1$。
   - 第二段：$x^{1/3} \le 2$ 且 $x \ge 1 \implies 1 \le x \le 8$。
4. **最终解集**：$x \in (0, 1) \cup [1, 8]$ 即 $(0, 8]$。
**考点**：幂函数与指数函数的复合不等式求解需注意定义域限制。

#### 5. 作业：分段函数不等式 32:06
- 分段函数不等式解题方法：当函数类型具体给出时，需采用与抽象型问题一致的解题思路。
- **对数处理原则**：对于涉及多项相乘、相除、开方或乘方的表达式，优先考虑两边取对数进行简化。
- **指数函数处理**：若指数函数的指数部分较为复杂，同样建议先取对数将指数降为乘积形式。

#### 6. 作业：二次不等式解集 32:45
- **二次不等式解题步骤**：
  1. **因式分解**：将不等式 $x^2 - 2ax - 8a^2 < 0$ 分解为 $(x+2a)(x-4a) < 0$（已知 $a > 0$）。
  2. **确定解集范围**：解集为 $x \in (-2a, 4a)$，其中 $x_1 = -2a, x_2 = 4a$。
  3. **利用条件求解**：根据 $x_2 - x_1 = 15$ 可得 $6a = 15$，最终解得 $a = 5/2$。