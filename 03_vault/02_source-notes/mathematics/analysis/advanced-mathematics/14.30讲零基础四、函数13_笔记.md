---
以下为 AI 生成的文稿笔记内容
---

### 一、例题：函数图像 00:07

54 题涉及三角公式推导，要求确定函数在区间 $[-a, a]$ 上的单调递减性并求 $a$ 的最大值。核心解法为将表达式 $a\sin x + b\cos x$ 通过三角恒等变形合并为单一三角函数形式：

- **变形步骤**：提出系数 $\sqrt{a^2 + b^2}$，转化为 $\sqrt{a^2 + b^2}\sin(x + \phi)$，其中 $\tan \phi = b/a$。
- **本题特例**：当 $a=1, b=1$ 时，表达式简化为 $\sqrt{2}\sin(x + \pi/4)$，进一步利用奇函数性质可改写为 $-\sqrt{2}\sin(x - \pi/4)$。
- **图像分析**：
  - 原函数 $y = \sin x$ 向右平移 $\pi/4$ 单位后，再沿 $x$ 轴对称翻转并纵向拉伸 $\sqrt{2}$ 倍。
  - **单调性判定**：在平移后的坐标系中，函数在 $[-\pi/4, \pi/4]$ 区间内单调递减，故 $a$ 的最大值为 $\pi/4$。
- **方法对比**：直接通过图像变换比求导更高效，关键考点为三角恒等变形与数形结合能力。

### 二、例题：函数对称与求导 09:46

分析函数 $f(x) = \sin x + \frac{1}{\sin x}$ 的性质：

| 命题 | 结论 | 依据 |
| :--- | :--- | :--- |
| 奇偶性 | 奇函数 | $f(-x) = -f(x)$，定义域对称且 $x \ne k\pi$ |
| 对称性 | 关于 $x = \pi/2$ 对称 | 验证 $f(\pi/2 + x) = f(\pi/2 - x)$ 成立 |
| 最小值 | 非 $2$ | $\sin x$ 可为负，基本不等式前提不满足 |

- **易错点**：误用基本不等式需注意 $\sin x > 0$ 的条件。

### 三、例题：图像变换分析 14:54

56 题考察单位圆上几何关系与函数图像转换：

- **几何建模**：
  - **步骤 1**：角 $x$ 的终边 $OP$ 与定点 $A$ 构成三角形，通过垂线 $PM, OM$ 建立关系，$PM = \sin x, OM = \cos x$。
  - **步骤 2**：利用面积关系导出 $f(x) = \sin x \cos x = \frac{1}{2}\sin 2x$ ($0 < x < \pi/2$)。
- **分段扩展**：
  - 当 $\pi/2 < x < \pi$ 时，$f(x) = -\frac{1}{2}\sin 2x$（因 $\sin 2x < 0$ 时，函数值需取正）。
- **图像特征**：
  - **关键点**：$x = \pi/4$ 时 $f(x) = 1/2$，$x = \pi/2$ 时 $f(x) = 0$。
- **思维训练**：强调几何关系向解析式的转化能力。

### 四、例题：经典分析过程与结论 25:44

例题 57 的证明过程涉及单位圆几何分析法，证明 $\sin x < x < \tan x$ 在 $x \in (0, \pi/2)$ 成立：

- **几何关系证明**：
  - **面积比较法**：
    - 三角形 $OBC$ 面积 = $\frac{1}{2}\sin x$。
    - 扇形 $OBC$ 面积 = $\frac{1}{2}x$。
    - 显然：三角形 $OBC$ 面积 < 扇形 $OBC$ 面积 $\implies \sin x < x$。
  - **切线构造法**：
    - 过点 $C$ 作切线交 $OB$ 延长线于 $A$，则 $AC = \tan x$。
    - 三角形 $OAC$ 面积 = $\frac{1}{2}\tan x >$ 扇形 $OBC$ 面积 $\implies x < \tan x$。
- **最终结论**：$\sin x < x < \tan x$。
- **应用：夹逼准则**：当 $x \to 0$ 时，$\frac{\sin x}{x} \to 1$。

### 五、例题：画出函数图像求范围 35:07

例题 58 的解法需结合反三角函数图像性质：

- **函数定义域与值域**：
  - $\arcsin x$ 值域为 $[-\pi/2, \pi/2]$。
  - $\arccos x$ 值域为 $[0, \pi]$。
- **图像交点分析**：两曲线在 $x = \sqrt{2}/2$ 处相交（对应角度 $\pi/4$）。
- **不等式解集**：$\arcsin x > \arccos x$ 成立的 $x$ 范围为 $(\sqrt{2}/2, 1]$。

### 六、例题：三角函数与反三角函数计算 38:26

例题 59 通过具体数值计算比较 $m, n, k$ 的大小：

- **变量计算过程**：
  - $m = \cos(2 \arcsin \frac{1}{2}) = \cos(\pi/3) = 1/2$。
  - $n = \sin(\arccos \frac{\sqrt{2}}{2}) = \sin(\pi/4) = \sqrt{2}/2$。
  - $k = -\sqrt{\frac{1-\cos(\arctan 2\sqrt{2})}{2}}$（此处需核对原公式，通常涉及半角公式）。
- **大小关系结论**：$n > m > k$，对应选项 B。

### 七、例题：反函数取值原则 45:34

**反函数取值原则**：在具有反函数的区间上才能进行反函数运算。以 $y = \arcsin x$ 为例，其主值区间为 $[-\pi/2, \pi/2]$。

**反函数推导过程**：
1. **定义**：若 $y = \arcsin x$，则 $x = \sin y$，且 $y \in [-\pi/2, \pi/2]$。
2. **奇偶性证明**：对 $x = \sin y$ 取负得 $-x = -\sin y = \sin(-y)$。由于 $-y \in [-\pi/2, \pi/2]$，可取反正弦得 $\arcsin(-x) = -y = -\arcsin x$，证得 $\arcsin x$ 为奇函数。

**$\arccos x$ 的性质分析**：
- **主值区间**：$y = \arccos x$ 的主值区间为 $[0, \pi]$。
- **关系式**：可证得 $\arcsin x + \arccos x = \pi/2$。
- **作业**：同理可验证 $\sin(\arccos x) = \sqrt{1-x^2}$。

### 八、例题：反函数图像变换 57:39

**复合函数 $g(f(x))$ 与 $f(g(x))$ 的区别**：
- $g(f(x)) = \tan(\arctan x) = x$。由于 $\arctan x \in (-\pi/2, \pi/2)$ 与 $\tan u$ 的定义域一致，图像为直线 $y = x$。
- $f(g(x)) = \arctan(\tan x)$：
  - **周期性**：$\tan x$ 以 $\pi$ 为周期。
  - **主值区间**：在 $(-\pi/2, \pi/2)$ 内，$\arctan(\tan x) = x$。在 $x = \pi/2 + k\pi$ 处无定义。
  - **图像特征**：在 $(-\pi/2, \pi/2)$ 区间为直线 $y = x$，其余区间通过周期性延拓。

**关键结论**：
- **函数顺序影响**：反函数与原函数的复合顺序不同会导致表达式及图像差异。
- **定义域与周期性**：反三角函数的主值区间与原函数的周期性是分析此类问题的核心。