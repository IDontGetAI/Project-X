---
以下为 AI 生成的文稿笔记内容
---

### 一、二次函数的不等问题 00:02

#### 1. 二次函数的不等问题与一元二次方程、二次函数的关系及其解 00:03

##### 1) 二次函数、一元二次方程、一元二次不等式的关系总结 00:08

二次函数的不等问题与一元二次方程、二次函数密切相关。将一元二次方程的等零条件改为不等零后，问题复杂度显著提升。二次函数图像与 $x$ 轴的交点数量由判别式 $\Delta$ 决定：

- $\Delta > 0$：图像与 $x$ 轴有两个交点，对应方程有两个不等实根。
- $\Delta = 0$：图像与 $x$ 轴有一个交点，对应方程有两个相等实根。
- $\Delta < 0$：图像与 $x$ 轴无交点，对应方程无实根。

一元二次不等式的解集与二次函数图像的关系（假设 $a > 0$）：

- **$\Delta > 0$**：
  - 不等式 $f(x) > 0$ 的解集为 $x < x_1$ 或 $x > x_2$。
  - 不等式 $f(x) < 0$ 的解集为 $x_1 < x < x_2$。
- **$\Delta = 0$**：
  - 不等式 $f(x) > 0$ 的解集为 $x \ne -\frac{b}{2a}$。
  - 不等式 $f(x) < 0$ 的解集为空集 $\varnothing$。
- **$\Delta < 0$**：
  - 不等式 $f(x) > 0$ 的解集为全体实数 $\mathbb{R}$。
  - 不等式 $f(x) < 0$ 的解集为空集 $\varnothing$。

##### 2) 分类讨论思想的重要性 04:42

分类讨论思想是数学解题的核心方法之一。在一元二次不等式问题中，需根据判别式 $\Delta$ 的三种情况（$\Delta > 0, \Delta = 0, \Delta < 0$）分别讨论解集。考研真题中常涉及分类讨论，例如参数不同取值对结果的影响。

#### 2. 一元二次方程在一个区间上有解的条件 06:25

区间根问题需讨论根的位置关系，主要涉及以下四类问题：
- 根的位置：与给定点 $k$ 的相对位置（如 $x_1 < x_2 < k$ 或 $k < x_1 < x_2$）。
- 函数值的正负：在特定区间内函数值的恒正或恒负。
- 最值问题：函数在区间内的最大值或最小值。

##### 1) 根的分布 09:16

- **根的分布情况：$x_1 < x_2 < k$** 09:19
  充要条件：
  - $\Delta > 0$（确保有两个不等实根）。
  - $f(k) > 0$（$k$ 点函数值为正）。
  - $k > -\frac{b}{2a}$（$k$ 位于对称轴右侧）。

- **根的分布情况：$k < x_1 < x_2$** 11:58
  充要条件：
  - $\Delta > 0$。
  - $f(k) > 0$。
  - $k < -\frac{b}{2a}$（$k$ 位于对称轴左侧）。

- **根的分布情况：$x_1 < k < x_2$** 12:51
  充要条件：
  - $f(k) < 0$（无需 $\Delta > 0$，因开口向上时，函数值小于零已隐含存在两个实根）。

- **根的分布情况：$x_1, x_2 \in (k_1, k_2)$** 15:04
  根的分布分析需满足以下条件：
  - **函数值条件**：$f(k_1) > 0$ 且 $f(k_2) > 0$。
  - **对称轴限制**：$k_1 < -\frac{b}{2a} < k_2$，确保区间 $(k_1, k_2)$ 包含对称轴。
  - **判别式要求**：$\Delta \ge 0$，保证方程有实根。
  - **区间包含性**：若 $x_1 \ne x_2$，需严格满足 $\Delta > 0$；若 $x_1 = x_2$，则 $\Delta = 0$。

**注意**：仅 $f(k_1)f(k_2) > 0$ 无法保证根在区间内，需结合对称轴位置与判别式综合判断。

- **根的分布情况：$x_1, x_2$ 有且仅有一个在 $(k_1, k_2)$ 内** 19:30
  单根分布条件需分两种情况讨论：
  - **根在区间内且端点函数值异号**：$f(k_1)f(k_2) < 0$，此时必有一根 $\in (k_1, k_2)$，另一根 $\in (-\infty, k_1) \cup (k_2, +\infty)$。
  - **根与区间端点重合**：
    - 若 $f(k_1) = 0$，需满足 $k_1 < -\frac{b}{2a}$ 且 $k_2 > -k_1 - \frac{b}{a}$。
    - 若 $f(k_2) = 0$，需满足 $k_2 > -\frac{b}{2a}$ 且 $k_1 < -k_2 - \frac{b}{a}$。
  - **关键点**：开区间性质要求严格排除端点重合情况，需通过对称轴位置约束根的分布范围。

##### 2) 分类讨论 20:14
分类讨论核心原则：
- **不重不漏**：需覆盖所有可能情形，包括根在区间内、外及端点重合。
- **几何辅助**：结合抛物线图像分析对称轴、函数值与根的对应关系。

- **分类讨论的思想** 20:36
  分类讨论应用：
  - **情形 1**：根在区间内时，需满足 $f(k_1)f(k_2) < 0$。
  - **情形 2**：根与端点重合时，需额外限制对称轴位置及另一端点函数值。

- **几何分析** 21:08
  - **异号条件**：$f(k_1)f(k_2) < 0$ 保证曲线穿过区间，必有一根 $\in (k_1, k_2)$。
  - **单调性影响**：若抛物线开口向上且 $f(k_1) < 0$，则另一根必在 $(-\infty, k_1)$ 内。

- **零点定理** 22:01
  若 $f(x)$ 在 $[k_1, k_2]$ 连续且 $f(k_1)f(k_2) < 0$，则存在 $x_0 \in (k_1, k_2)$ 使 $f(x_0) = 0$。
  应用限制：
  - 单调函数：唯一零点。
  - 非单调函数：可能多个零点，需结合导数进一步分析。

- **例题 2：$x_1, x_2$ 有且仅有一个在 $(k_1, k_2)$ 内，$f(k_1)$ 或 $f(k_2)$ 等于 $0$ 的情况讨论** 26:32
  端点重合条件：

| 情形 | 条件 |
| :--- | :--- |
| $f(k_1) = 0$ | $k_1 < -\frac{b}{2a}$ 且 $k_2 > -k_1 - \frac{b}{a}$（确保 $x_2 \in (k_1, k_2)$ 且 $x_1 \notin (k_1, k_2)$） |
| $f(k_2) = 0$ | $k_2 > -\frac{b}{2a}$ 且 $k_1 < -k_2 - \frac{b}{a}$（确保 $x_1 \in (k_1, k_2)$ 且 $x_2 \notin (k_1, k_2)$） |

- **根的分布与图像** 37:53
  此类训练具有重要价值。通过零基础阶段对问题的深入理解，可将其作为优质训练素材。分类讨论思维需通过实际案例训练，而非仅停留在理论层面。中学阶段常见的二级结论在大学数学中并不适用，重点在于通过推导和分析掌握解题方法，而非机械记忆。

#### 3. 二次不等式在一个区间上恒成立的条件 39:58

二次不等式在区间上恒成立的条件可通过数形结合分析。

##### 1) 二次不等式与二次函数的基本概念 40:08
二次不等式形式为 $ax^2 + bx + c > 0$（或 $< 0$，其中 $a \ne 0$）。对于开区间 $(k_1, k_2)$，若要求不等式大于零，需函数图像在区间内完全位于 $x$ 轴上方。需分开口方向讨论：
- **$a < 0$ 时**：抛物线开口向下。
- **$a > 0$ 时**：抛物线开口向上。

##### 2) $a < 0$ 时二次不等式恒成立的条件 41:04
当 $a < 0$ 时，抛物线开口向下，需满足：
- $f(k_1) \ge 0$。
- $f(k_2) \ge 0$。
由于区间为开区间，端点函数值等于零不影响区间内函数值大于零。

##### 3) $a > 0$ 时二次不等式恒成立的条件：判别式小于零 43:23
当 $a > 0$ 时，抛物线开口向上，最简单情况为判别式 $\Delta < 0$，此时函数图像完全位于 $x$ 轴上方。

##### 4) $a > 0$ 时二次不等式恒成立的条件：对称轴在区间左侧 44:26
若对称轴 $x = -\frac{b}{2a}$ 位于区间左侧 ($\le k_1$)，需满足 $f(k_1) \ge 0$，因函数在区间内单调递增，$f(k_2)$ 必然更大。

##### 5) $a > 0$ 时二次不等式恒成立的条件：对称轴在区间右侧 45:10
若对称轴位于区间右侧 ($\ge k_2$)，需满足 $f(k_2) \ge 0$，因函数在区间内单调递减，$f(k_1)$ 必然更大。

#### 4. 二次函数在闭区间上的最值 45:39

##### 1) 二次函数在闭区间上的最值概述 45:41
二次函数 $f(x)$ 在闭区间上的最值问题需结合图像分析。

##### 2) 二次函数最值讨论：对称轴在区间内 46:04
当 $a > 0$ 且对称轴位于区间 $[m, n]$ 内时：
- **最小值**为顶点处函数值。
- **最大值**为 $f(m)$ 或 $f(n)$ 中的较大者。

##### 3) 二次函数最值讨论：对称轴在区间一侧 47:44
若区间完全位于对称轴一侧：
- 当 $n < -\frac{b}{2a}$ 时，$f(m)$ 为最大值，$f(n)$ 为最小值。
- 当 $m > -\frac{b}{2a}$ 时，$f(n)$ 为最大值，$f(m)$ 为最小值。

#### 5. 应用案例 48:59

##### 1) 例题：常用不等式证明 49:04
常用不等式包括：
- $a^2 + b^2 \ge 2ab$（可由算术平均 $\ge$ 几何平均或完全平方公式推导）。
- $\frac{a+b}{2} \ge \sqrt{ab}$ ($a, b > 0$)。

**例题分析**：已知 $a, b, c > 0$ 且 $a + b + c = 1$，证明：
- $ab + bc + ac \le 1/3$（通过平方和与乘积关系证明）。
- $\frac{a^2}{b} + \frac{b^2}{c} + \frac{c^2}{a} \ge 1$（通过添加辅助项 $b, c, a$ 后应用基本不等式）。

##### 2) 例题：$x$ 与 $y$ 的关系证明 01:02:55
例题 17 涉及以下证明过程：
- **第一问证明**：
  - 已知条件：$x + y = 1$。
  - 目标：证明 $xy \le 1/4$。
  - 过程：$\sqrt{xy} \le \frac{x+y}{2} = 1/2 \implies xy \le 1/4$。
- **第二问证明**：
  - 证明 $(1 + 1/x)(1 + 1/y) \ge 9$。
  - 展开：$1 + 1/x + 1/y + 1/(xy) = 1 + (x+y)/(xy) + 1/(xy) = 1 + 2/(xy)$。
  - 由于 $xy \le 1/4$，则 $1/(xy) \ge 4$，故 $1 + 2/(xy) \ge 1 + 8 = 9$。

**注意**：作业需独立完成，重点掌握不等式变形与代数替换的逻辑。