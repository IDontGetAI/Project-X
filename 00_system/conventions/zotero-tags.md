# Zotero 标签规范

使用受控前缀，避免同义标签泛滥。

- `subject/<main>/<branch>`：学科与分支。
- `status/<value>`：阅读或处理状态。
- `priority/<value>`：优先级。
- `kind/<value>`：资料性质。
- `method/<value>`：方法类型。

不要同时使用 `status/reading`、`state/reading`、`正在读` 这类同义标签。
