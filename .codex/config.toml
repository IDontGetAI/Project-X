# Project-level Codex safety defaults.
# This knowledge base treats local files as core assets, so the default policy is conservative.
approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "cached"

[sandbox_workspace_write]
network_access = false
writable_roots = []
